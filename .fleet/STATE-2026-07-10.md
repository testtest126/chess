# Fleet state — end of day 2026-07-10

## Mid-flight sequence (highest priority on resume)

**Deployment of chess-server to Fly.io — paused at the merge gate.**

- User approvals on record: "deploy on green; spend gate approved for the
  cents posture" (recorded on issue #28).
- PR #80 (deployment config + Fly-Client-IP limiter fix + SQLITE_PATH boot
  path) has **"Security review: APPROVE @ 5dbd52d"**; the Docker lane
  ("Server image builds") passed, machine-verifying the Dockerfile fix; the
  only red lane was the pre-existing #68 iOS flake (non-required, log-attributed).
  At day end it awaited Server tests + Analyze completing, then the
  orchestrator merges.
- **On resume, if #80 is merged, execute the runbook** (`deployment-runbook.md`)
  from post-merge main. If unmerged, ask the orchestrator session why.

## Verdict queue (root agent = security reviewer)

1. PR #78 (SIWA nonce replay + first-link race, closes #53) @ head **5533658**
   — review AFTER the deploy. Re-check the TOCTOU thread as resolved by the
   atomic `DELETE...RETURNING`; note the added live-path test (genuinely
   signed token WITHOUT nonce binding must be refused).

## Open threads

- #68 red iOS lane: fix in PR #75 (readiness-gated taps), plus #76 (macos-26 /
  Xcode 26 lane). Recommend making the iOS lane a required check once green.
- #82 (SQLITE_PATH in production): implemented inside PR #80 — close #82 when
  #80 merges if not auto-closed.
- Apple/SIWA production config still needs the user: Team ID, App ID
  registration, capability, SIWA_APP_ID secret (endpoint 503s until then).
- iOS release builds point at https://matemate-chess.fly.dev (PR #80's
  ServerConfig change).

## Fly resources at day end

- App `matemate-chess`: **zero machines / IPs / volumes**; one staged secret
  (JWT_SECRET — ROTATE before deploy, commitment on record).
- `matemate-chess-db` destroyed (audit on #28). No other billable resources.

## Today in one line

~25 PRs merged by a 5+ session fleet: rematch + decline + expiry, time
controls, engine TT/pondering + memory bound, JWT 5, SIWA + 2 security-fix
rounds + hardening in review, iOS CI + stability work, localization, player
profiles, game history, resume banner, bot difficulty, coordination charter
v1→v2 — plus the incident record that produced the rules in CLAUDE.md.
